<label></label><input type="checkbox"  onclick="clickAll('logversion')">click all</label>
#foreach($version in $versions)
<label><input type="checkbox" class="logversion form-check-input toggle" ic-put-to="/version/${version.getId()}/toggle" #if($version.active)checked#end>${version.getVersion()}</label>
#end